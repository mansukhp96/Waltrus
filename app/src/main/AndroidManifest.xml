<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.izmansuk.securepasswordmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SecurePasswordManager">
        <activity android:name=".ui.main.EditCredsActivity"/>
        <activity android:name=".ui.main.AddCredsActivity" />
        <activity
            android:name=".ui.login.AddCredsActivity"
            android:label="@string/title_activity_add_creds" />
        <activity android:name=".SetMPasswordActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".AboutActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name_short"
            android:theme="@style/Theme.SecurePasswordManager.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>